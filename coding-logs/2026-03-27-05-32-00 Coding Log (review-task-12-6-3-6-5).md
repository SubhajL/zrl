# Coding Log

## Review (2026-03-27 05:32 +07) - tasks 12, 6.3, 6.5

### Reviewed
- Repo: /Users/subhajlimanond/dev/zrl
- Branch: main
- Scope: commits `371c14c`, `0918a43`, `0eb4d7f`, `455a387`, `50630a8` plus current `main` implementation for Task 12, Task 6.3, and Task 6.5
- Commands Run: `git status --short`; `git log -n 20 --oneline --decorate`; `git show --stat --name-only 371c14c 0918a43 0eb4d7f 455a387 50630a8`; `node` Handlebars render check against `templates/regulator.hbs`, `templates/buyer.hbs`, `templates/defense.hbs`; `npm run test -- --runInBand src/modules/lane/lane-timeline.spec.ts src/modules/lane/lane.service.spec.ts src/modules/evidence/proof-pack.service.spec.ts`; `npm run test:e2e -- --runInBand test/lane.e2e-spec.ts test/proof-pack.e2e-spec.ts`

### Findings
CRITICAL
- Real proof-pack generation is broken by the templates as committed. `src/modules/evidence/proof-pack.service.ts:122` compiles raw Handlebars without registering helpers, but all three templates use the `eq` helper (`templates/regulator.hbs:274`, `templates/buyer.hbs:319`, `templates/defense.hbs:273`). Direct render verification failed with `Missing helper: "eq"` for all three templates. Fix direction: register the helper set centrally before compile or remove helper-dependent syntax. Test needed: render each real template file, not only simplified inline templates.
- The stored proof-pack hash is for the wrong bytes. `src/modules/evidence/proof-pack.service.ts:52-62` hashes the first-pass PDF, then renders and uploads a different final PDF. The final document embeds `{{contentHash}}` (`templates/regulator.hbs:393`, `templates/buyer.hbs:426`, `templates/defense.hbs:497`), so the uploaded bytes cannot match the stored digest. Fix direction: hash the exact uploaded buffer or redesign the verification marker so it does not mutate the hashed payload. Test needed: generate a pack, read the stored object back, and assert its digest equals `content_hash`.

HIGH
- The QR-code fix is incomplete: the QR image still encodes the placeholder, not the final verification value. `src/modules/evidence/evidence.controller.ts:456-480` generates `qrCodeDataUrl` once from `hash-will-be-computed`, while `src/modules/evidence/proof-pack.service.ts:57-60` only replaces `contentHash` on the second pass. The templates render the QR from `qrCodeDataUrl` (`templates/regulator.hbs:386`, `templates/buyer.hbs:419`, `templates/defense.hbs:490`). Fix direction: regenerate the QR after the final verification value is known. Test needed: decode the embedded QR payload and assert it matches the intended verification target.
- Task 12 remains only partially implemented against its task contract. `src/modules/evidence/evidence.controller.ts:359-500` exposes lane-scoped generate/list routes only; the task still calls for metadata, download, and public verification endpoints plus async generation semantics. Fix direction: either ship the missing API surface before marking Task 12 complete or keep Task Master/commit messaging explicit that this is partial delivery. Test needed: end-to-end coverage for metadata/download/verify flows.
- Task 6.3 is not implemented to its stated scope. The task calls for recording a new checkpoint with multipart photo/temp/signature, but the controller only ships list and update routes in `src/modules/lane/lane.controller.ts:465-487`. There is no `POST /lanes/:id/checkpoints`, no multipart handling, and no evidence-artifact creation path. Fix direction: implement the create flow or keep the subtask labeled partial. Test needed: e2e checkpoint creation covering checkpoint row, photo/signature evidence, and audit side effects.

MEDIUM
- Task 6.5 is only a thin audit projection, not the richer lane timeline described by the task. `src/modules/lane/lane.service.ts:413-427` just maps audit entries, and `src/modules/lane/lane.types.ts:300-308` exposes no event-specific metadata. That leaves no room for checkpoint details, pack version info, completeness deltas, or other timeline metadata the task calls for. Fix direction: enrich events by kind or add typed metadata per event source. Test needed: mixed timeline coverage with evidence, checkpoint, transition, and pack-generation events.
- Checkpoint audit entries omit most of the changed checkpoint data. `src/modules/lane/lane.service.ts:394-400` hashes only `{ laneId, checkpointId, status }`, even though checkpoint updates can also change timestamp, temperature, GPS, signature, signer, and notes (`src/modules/lane/lane.types.ts:115-124`). Two materially different updates can therefore share the same semantic audit payload. Fix direction: hash the full normalized checkpoint snapshot or full update payload. Test needed: assert temperature-only and signature-only changes alter the audit payload hash.

LOW
- Task Master tracking is stale. As of 2026-03-27, Task `12` and subtasks `6.3` / `6.5` still show `pending` even though `main` already contains the related commits. Update the task statuses only after the scope gaps above are resolved or explicitly accepted as partial delivery.

### Open Questions / Assumptions
- I treated this as a review of shipped code on `main`, not a review of an unmerged working tree.
- I assumed the proof-pack verification contract intends the stored `content_hash` to match the exact bytes retrievable from object storage.
- I assumed Task Master subtask text remains the source of truth for whether `6.3` and `12` are actually complete.

### Recommended Tests / Validation
- Add a proof-pack integration test that renders each real template file and fails on missing helpers.
- Add a storage-backed proof-pack test that re-reads the uploaded object and verifies its digest matches the stored `content_hash`.
- Add QR verification coverage that decodes the generated QR and asserts the expected verification target.
- Add a checkpoint creation e2e test for `POST /lanes/:id/checkpoints` once implemented, including photo/signature evidence and audit assertions.
- Add a timeline e2e test with mixed event types and event-specific metadata assertions.
- Add a checkpoint audit regression test proving non-status field changes produce distinct payload hashes.

### Rollout Notes
- Do not mark Task 12 as done until the pack verification path is trustworthy; the current helper/hash/QR issues undermine the core proof-pack integrity claim.
- Do not mark Task 6.3 as done until checkpoint creation exists or the subtask is explicitly re-scoped to read/update only.
- Do not mark Task 6.5 as done until the event payload is rich enough for downstream consumers, or the task description is narrowed to “audit timeline”.
