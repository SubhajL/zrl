# ZRL Development Progress

## Session Log

<!-- Append entries here. Format: date | what was done | what's next -->

2026-04-13 13:15 ICT | Fixed `prisma/seed.ts` reseed ordering so Playwright local startup can delete `evidence_artifact_analyses` before `evidence_artifacts`; repeated `db:seed`, the full OCR matrix Playwright suite, and a follow-up smoke run now pass from the clean PR branch | Push the seed fix, refresh the PR, and merge to `main`
2026-03-21 13:17 ICT | Installed global Claude hooks, connected project-scoped taskmaster-ai in core mode, and created Coding Log scaffolding | Initialize Task Master tasks from docs/PRD.md
2026-03-21 13:24 ICT | Added root AGENTS.md tailored to the current docs-first repo and existing harness | Add subdirectory AGENTS.md files after real scaffolding exists
2026-03-21 13:48 ICT | Added three Codex Task Master skills in `~/.codex/skills` for init, next-task, and status workflows | Forward-test the new skills against the ZRL repo workflow
2026-03-21 14:08 ICT | Added and tested a `taskmaster-expand-task` skill; confirmed forced expansion can time out and clear subtasks in an isolated temp repo | Treat `force=true` as an overwrite path and use it only deliberately
2026-03-21 14:23 ICT | Added `taskmaster_tasks_guard.py` backup/restore helper to the expand-task skill and verified snapshot/restore round-trip on a temp Task Master repo copy | Use the helper before any forced subtask expansion
2026-03-21 14:36 ICT | Hardened Task Master MCP launch via a pinned wrapper script and stderr logging in `.taskmaster/logs/` | Restart or reload MCP clients so they pick up the new `.mcp.json`
2026-03-21 13:40 ICT | Task 1 done: Project scaffolding — NestJS backend (6 modules + 3 common services wired into AppModule), Next.js 16 frontend with ZRL design tokens, TypeScript strict mode, Prisma ORM, Docker Compose (PostgreSQL 15 + Redis 7). All quality gates pass (build, typecheck, lint, test) | Start Task 2 (Database Schema)
2026-03-21 14:25 ICT | Filtered Task Master startup-only stdout notifications via a repo-local MCP proxy so `claude mcp list` now shows `taskmaster-ai` connected again | Keep the proxy behavior pinned unless a future Task Master release stops emitting those startup notifications
2026-03-21 14:47 ICT | Refreshed root guidance for the scaffolded repo and added local `AGENTS.md` files for `src/`, `frontend/`, `prisma/`, and `test/` | Add deeper local guidance only when those areas grow stable sub-structures
2026-03-21 14:56 ICT | Added deterministic `AGENTS.md` sync automation plus project Claude hooks so root and local guides auto-refresh from repo state and future `rules/` or `templates/` guides can be bootstrapped when those directories become real | Keep managed sections marker-based and let humans own the non-generated policy text
2026-03-21 16:35 ICT | Task 2 done: Prisma schema with 11 models, 16 enums, snake_case mappings, SQL CHECK constraints (SHA-256 hex + MFA enforcement), seed script (4 users, 1 Mango→Japan lane, 12 critical MRL substances, 2 hash-chained audit entries). All quality gates pass. | Start Task 3 (SHA-256 Hashing Service)
2026-03-21 16:41 ICT | Added a repo-local Claude auto-format hook for TS/JS/JSON/CSS/Markdown/YAML and Prisma files, and wired it into project `PostToolUse` so this repo now auto-formats on `Edit|Write` alongside the existing AGENTS/CLAUDE advisory hooks | Keep the local hook behavior aligned with the global Claude formatter matrix
2026-03-21 16:45 ICT | Added repo-local Codex hook emulation via `.codex/hooks/codex_hooks.py` plus `scripts/codex-with-hooks.sh`, so Codex sessions can reuse the existing format, CLAUDE-staleness, and AGENTS-sync hook logic even though Codex CLI here has no native `hooks` block | If desired, add a shell alias so plain `codex` launches the repo-local hook wrapper automatically
2026-03-21 16:52 ICT | Added a global zsh `codex()` shim that automatically routes plain `codex` through `scripts/codex-with-hooks.sh` only for the ZRL repo (including `codex -C /Users/subhajlimanond/dev/zrl ...`) and leaves all other directories on the real `/opt/homebrew/bin/codex` binary | If you want the same pattern for other repos, factor the zsh shim into a shared helper
2026-03-21 18:39 ICT | Generalized the global zsh `codex()` shim so any git repo with an executable `scripts/codex-with-hooks.sh` now gets automatic Codex wrapper routing, while plain directories and repos without that launcher still use the real `/opt/homebrew/bin/codex` binary | Reuse the same launcher convention in any repo that wants Codex-side hook emulation
2026-03-21 19:57 ICT | Task 3 done: added a shared SHA-256 `HashingService` with stream hashing, artifact digest verification, canonical audit-chain helpers, and unit tests; also aligned `prisma/seed.ts` to reuse the same hash-chain computation instead of open-coded concatenation | Start Task 4 (Tamper-Evident Audit Trail Service)
2026-03-21 20:58 ICT | Task 4 done: implemented the tamper-evident audit trail service with lane-scoped append-only hashing, advisory-lock concurrency protection, query/verify/export endpoints, and metadata-gated automatic audit logging; all backend gates and focused audit e2e coverage pass | Start Task 13 (Fruit Temperature Profiles) or Task 5 if strict security/auth flow takes priority
2026-03-22 11:36 ICT | Task 31 CI/CD/deployment config tightened: CI now uses repo scripts for Prisma/test/build steps, no longer masks E2E or audit failures, added backend/frontend Dockerfiles, compose app services, and a GHCR publish workflow; frontend/container validation passed while backend checks remain blocked by the in-flight Task 5 auth branch | Finish Task 5 and rerun the backend gates + backend image build on the merged tree
2026-03-22 11:39 ICT | Task 5 done: implemented JWT login/refresh/logout, MFA enrollment and verification, RBAC guards/decorator metadata, partner API key validation, and auth wiring/tests; backend lint/typecheck/test/build plus auth e2e all pass | Let the integration worker reconcile Task 31's workflow/container changes on the combined tree
2026-03-22 12:53 ICT | Landed the core Task 6 lane CRUD module and Task 8 rules engine in one integrated tree: new `/lanes` and `/rules` controllers/services/stores, Japan mango YAML rules, additive rules-store Prisma models/migration, rule snapshoting on lane create, and automatic rule-version bumps on admin substance changes. Backend `db:generate`, lint, typecheck, test, build, and focused lane/rules e2e all pass | Decide whether to extend lane-scoped audit semantics to cover global rule/substance changes before marking Task 8 fully complete and committing
2026-03-22 15:30 ICT | Closed the Task 8 audit follow-up: rule/substance admin mutations now write append-only entries to a dedicated global rules audit stream inside the same transaction as the mutation and version bump, with controller/e2e coverage for authenticated actor propagation. `db:generate`, lint, typecheck, test, build, and focused audit/rules/lane e2e all pass | Commit and submit the integrated Task 6/8 batch when ready
2026-03-22 15:48 ICT | Hardened the Task 6/8 submission branch before review: exporter lane updates now enforce ownership before persistence, and the full backend gate set still passes in a clean feature worktree isolated from unrelated frontend changes | Stage, commit, and submit the isolated Task 6/8 PR
2026-03-22 20:13 ICT | Implemented the Task 10 contract-facing evidence module slice in an isolated worktree: JWT evidence list/upload/detail/verify/graph routes, partner JSON ingestion, hash-based local object storage, append-only artifact audit entries, additive evidence schema/migration, and focused evidence unit/e2e coverage. `db:generate`, lint, typecheck, test, and build all pass | Finish the remaining Task 10 infrastructure gaps (real S3 adapter and photo EXIF extraction) before marking the task fully done in Task Master
2026-03-23 11:11 ICT | Finished the remaining Task 10 evidence backend scope in the isolated worktree: optional S3 object-store adapter, EXIF/GPS checkpoint-photo metadata extraction, Task 25 evidence response mapping, and `DELETE /evidence/:id` soft delete with artifact audit entry. `db:generate`, lint, typecheck, focused evidence unit/e2e, full test, and build all pass | Mark Task 10 done in Task Master and stage the evidence branch for commit/review
2026-03-23 14:56 ICT | Implemented Task 7 lane lifecycle enforcement in the isolated worktree: added `POST /lanes/:id/transition`, service-level transition guards for completeness/proof-pack/archive retention, additive `lanes.status_changed_at` schema support, and focused lane unit/e2e coverage. `db:generate`, lint, typecheck, full test, build, and repeated focused flake runs all pass | Stage the Task 7 branch for review/commit once the coding log review artifact is appended
2026-03-23 14:40 ICT | Task 13 cold-chain profiles: store-backed fruit profile service/controller tests now cover the four canonical fruit profiles plus lane cold-chain config validation, and the cold-chain routes are wired through the Nest app. `typecheck`, `build`, focused cold-chain unit tests, and cold-chain e2e all pass | If needed, add lane-scoped temperature readings/excursions as a follow-up subtask
2026-03-24 05:26 ICT | Task 9 validation engine implemented in the isolated Task 9 worktree: rules-engine now computes checklist/completeness/MRL validation and certification expiry alerts, `GET /lanes/:id/completeness` returns checklist-driven output, `GET /rules/markets/:market/checklist?product=` is wired, and evidence uploads now resync `lanes.completeness_score`. `db:generate`, lint, typecheck, focused Task 9 unit/e2e, full test, and build all pass | Commit/review the Task 9 worktree when ready; a future notification-service scheduler can extend expiry reminders beyond immediate detection
2026-03-24 05:04 ICT | Task 11 done in an isolated worktree: evidence uploads now auto-create predecessor links, the evidence graph route returns verification-aware DAG data, and `POST /lanes/:id/evidence/graph/verify` re-hashes all lane artifacts and audits the lane-level verification action. `db:generate`, lint, typecheck, focused evidence unit/e2e, full test, and build all pass | Start Task 9 or Task 14 depending on whether validation or temperature ingestion takes priority next
2026-03-24 06:41 ICT | Task 7.3 automatic status transitions implemented in an isolated worktree: evidence uploads now trigger lane lifecycle reconciliation after completeness sync, lanes can auto-move `INCOMPLETE -> EVIDENCE_COLLECTING -> VALIDATED`, and the upload path now fails open if post-commit reconciliation errors occur. Focused lane/evidence unit tests, 3x flake reruns, lint, typecheck, full test, and build all pass | Run formal review/commit on the Task 7.3 worktree when ready
2026-03-24 07:45 ICT | Task 14.1-14.3 finished in the isolated Task 14 worktree: lane-scoped JSON/CSV temperature ingestion, persisted readings plus excursions, downsampled lane temperature reads, and shelf-life/SLA reporting all validate cleanly. `db:generate`, focused cold-chain unit/e2e, lint, typecheck, full test, and build all pass; Task Master now shows `14.1`-`14.3` done while `14.4` remains pending | Review/commit the Task 14 worktree, then decide whether to start alert delivery for Task 14.4 or move to another pending backend task
2026-03-27 05:46 ICT | Fixed the critical proof-pack integrity path: shipped Handlebars templates now compile through a local helper-aware renderer, proof packs render only once, and `proof_packs.content_hash` now matches the exact bytes written for upload; focused proof-pack unit tests pass 3x, proof-pack e2e passes, and backend typecheck/lint/build are green | Tackle the remaining non-critical proof-pack gaps next, starting with public verify/download endpoints and richer QR semantics
2026-03-27 05:50 ICT | Added proof-pack metadata, download, and public verify routes backed by direct pack lookup and object-store re-hash verification; focused proof-pack unit tests pass 3x, proof-pack e2e passes, and backend typecheck/lint/build are green | Decide whether the next slice should finish async pack-generation semantics or move to the remaining lane-task gaps (6.3 checkpoint creation / 6.5 richer timeline metadata)
2026-03-27 06:09 ICT | Added the missing multipart checkpoint-capture route: `POST /lanes/:id/checkpoints` now records or creates a checkpoint by sequence, uploads checkpoint photo/signature artifacts through the evidence pipeline, and finalizes the checkpoint with signature hash and capture metadata; focused lane unit tests pass 3x, lane e2e passes, and backend typecheck/lint/build are green | Finish the remaining lane/proof-pack gaps, starting with either `GET /checkpoints/:cpId`, richer lane timeline metadata (6.5), or async proof-pack generation semantics
2026-03-27 06:23 ICT | Tightened checkpoint audit hashing to use full checkpoint snapshots and enriched `GET /lanes/:id/timeline` with typed lane/checkpoint/artifact/proof-pack metadata; focused lane service + timeline unit tests pass, and backend typecheck/lint/build are green | Remaining higher-cost follow-up is the proof-pack async generation contract, with `GET /checkpoints/:cpId` still missing as a smaller API gap
2026-03-27 06:47 ICT | Closed the remaining task-12/6.3/6.5 gaps: proof-pack generation now uses async `GENERATING/READY/FAILED` status records with owner-scoped pack routes, `GET /checkpoints/:cpId` is wired with checkpoint ownership enforcement, and lane timeline metadata now prefers historical audit snapshots with current-state fallback for older rows. Focused audit/auth/proof-pack/lane unit tests, focused lane/proof-pack e2e, `db:generate`, typecheck, lint, and build all pass | Remaining follow-up is operational: apply the new Prisma migration in shared environments before relying on async pack states or audit snapshots outside local code/test flows
2026-03-27 09:38 ICT | Added stale proof-pack recovery: `ProofPackService` now sweeps `GENERATING` packs older than 5 minutes on startup and every 60 seconds, marking orphaned rows `FAILED` with audit evidence so crashes/restarts do not leave packs stuck forever. Focused proof-pack unit tests, proof-pack e2e, typecheck, lint, and build all pass | Long-term follow-up is still a durable queue/worker if proof-pack generation needs multi-instance or retry-safe execution
2026-03-27 11:39 ICT | Replaced the stopgap proof-pack recovery path with a durable DB-backed job queue: persisted `proof_pack_jobs` payloads, worker leasing/heartbeat/retry semantics, bounded retry exhaustion, admin/auditor metrics, and a real Postgres-backed Nest restart test now prove expired in-flight jobs are reclaimed and completed after reboot. Also fixed the raw audit store to write `audit_entries.id` explicitly. `db:generate`, `prisma migrate deploy`, focused audit/proof-pack unit tests, focused proof-pack e2e/integration, lint, typecheck, and build all pass | Remaining long-term hardening is multi-worker throughput tuning and richer operational alert delivery if queue volume grows
2026-03-27 20:00 ICT | Task 5.5 password reset flow is now wired end-to-end in the auth module: added `POST /auth/forgot-password` and `POST /auth/reset-password`, persisted single-use reset requests with 1-hour expiry and 3-per-hour email throttling, revoked superseded tokens, and invalidated active sessions on successful reset. `db:generate`, focused auth unit/e2e, lint, typecheck, and build all pass | Follow up with real mail delivery once the notification/email infrastructure is in place; current non-production flows expose a preview token for local/test use
2026-03-27 21:16 ICT | Task 8.2 is now fully closed: Japan mango MRL seeding moved from a hardcoded 12-row list to CSV-backed rule data under `rules/japan/`, the runtime rule loader now resolves YAML + CSV sources and refreshes on CSV changes, and `prisma/seed.ts` now consumes the same file-backed definition as the rules engine. Focused rules-engine tests, lint, typecheck, and build all pass | Move to Task 33 next unless you want to extend the Japan dataset beyond the initial 12 critical substances immediately
2026-03-27 21:37 ICT | Task 33 shared database pool refactor is done: added a `DatabaseModule` with a singleton `pg.Pool` provider, `DB_POOL_SIZE` support (default 20), and shared lifecycle shutdown; audit/auth/lane/evidence/proof-pack/rules-engine/cold-chain stores now inject the same pool instead of creating seven independent pools. Focused database-module tests, auth AppModule e2e, lint, typecheck, and build all pass | Next solid backend step is Task 18 unless you want to submit/merge this batch first
2026-03-27 22:33 ICT | Task 18 notification service is now wired end-to-end: added durable `notifications` + `notification_preferences` tables, a new `NotificationModule` with JWT-protected `/notifications` APIs, authenticated `notification.new` websocket fanout, preference-aware multi-channel dispatch (in-app always, SES/LINE/push config-gated), and live producer hooks from proof-pack completion, cold-chain excursion ingestion, and rules-engine substance updates. `db:generate`, focused notification/proof-pack/cold-chain/rules-engine unit tests, notifications/auth/proof-pack e2e, lint, typecheck, and build all pass | Next solid backend step is Task 9.4 or 14.4 on top of the new notification substrate
2026-03-28 04:35 ICT | Closed the remaining Task 18 notification gaps: added per-user `notification_channel_targets`, authenticated `GET/PUT /notifications/channel-targets`, Redis-backed `notification.created` pubsub with local fallback fanout, and target-aware LINE/push delivery so cross-instance websocket fanout and explicit channel routing no longer depend on instance-local state or placeholder recipient mapping. `db:generate`, focused notification unit tests, notifications e2e, lint, typecheck, and build all pass | Next backend step is Task 9.4 or 14.4 on top of the hardened notification substrate
2026-03-28 07:40 ICT | Task 9.4 certification expiry alerting is now implemented: certification uploads trigger immediate owner notifications for expired or expiry-metadata-missing `PHYTO_CERT`/`VHT_CERT`/`GAP_CERT` artifacts, the rules engine now runs a daily certification-expiry worker for durable `30/14/7` warnings plus expired-cert catchup, and duplicate alerts are suppressed through additive `certification_alert_deliveries` claim rows. `db:generate`, focused evidence/rules-engine/worker unit tests, lint, typecheck, build, and focused evidence/rules-engine e2e all pass; the local e2e smoke was run with background workers disabled because the harness still boots without a DB-backed worker pool | Next solid backend step is Task 14.4 or Task 19 on top of the notification substrate
2026-03-28 11:31 ICT | Task 6.6 public lane ID lookup is now closed: lane ownership, lane store hydration/mutations, audit lane streams, cold-chain lane-scoped reads/writes, and lane-scoped evidence/proof-pack routes now accept either internal lane row IDs or public `LN-...` IDs while canonicalizing downstream queries to the internal lane ID. Focused auth/audit/lane/cold-chain unit tests, evidence e2e, lint, typecheck, and build all pass | Next solid backend step is Task 14.4 now that parent Task 6 is fully closed
2026-03-28 11:58 ICT | Task 14.4 excursion alerting is now implemented on top of the notification substrate: cold-chain ingestion now emits severity-aware `EXCURSION_ALERT` notifications, publishes authenticated `temperature.excursion` websocket events through Redis-backed fanout with direct fallback, and persistently marks lanes with additive `cold_chain_sla_breached_at` when critical excursions are detected. Focused cold-chain/notification unit tests, lint, typecheck, and build all pass | Next solid backend step is Task 19 unless you want to expose the new lane breach flag in lane detail/read models immediately
2026-03-28 12:06 ICT | Deep-confidence pass for Task 14.4 is complete: applied the live Prisma migration to local Postgres, verified `lanes.cold_chain_sla_breached_at` exists, added a real Postgres-backed `PrismaColdChainStore` spec, and fixed a surfaced production bug where raw `temperature_readings` and `excursions` inserts were missing explicit UUID ids even though the SQL tables have no DB-level defaults. DB-backed cold-chain store tests plus the focused notification/cold-chain suite, lint, typecheck, and build all pass | Residual gap is only the absence of a full HTTP e2e harness against a real DB for `/lanes/:id/temperature`
2026-03-28 12:36 ICT | Closed the last Task 14.4 confidence gap with a real Postgres-backed `/lanes/:id/temperature` HTTP e2e that boots `AppModule`, keeps auth guards active, proves persistence of readings/excursions/notifications plus `cold_chain_sla_breached_at`, and surfaced then fixed a production notification-store SQL bug (`users.id` qualification in `listDeliveryTargets`). Focused cold-chain/notification unit tests, mocked + live cold-chain e2e, lint, typecheck, and build all pass | Next solid backend step is Task 19 unless you want to expose the lane breach timestamp in lane detail/read models now
2026-03-28 13:58 ICT | Task 19 websocket realtime events is now implemented: the existing `/ws` gateway gained JWT-authenticated lane subscribe/unsubscribe with canonical lane-room membership, Redis-backed generic lane/user realtime fanout, a focused `RealtimeEventsService`, and producer hooks for lane status changes, checkpoint recordings, evidence uploads, proof-pack completion, rule updates, and temperature excursions. Focused notification/lane/evidence/proof-pack/rules/cold-chain unit suites plus notifications REST+websocket e2e, lint, typecheck, and build all pass | Remaining follow-up is test-harness hygiene: the older no-DB `lane.e2e` / `evidence.e2e` AppModule suites still boot background workers without configured DB-backed stores, so broader websocket route smoke should be added after that baseline harness issue is cleaned up
2026-03-28 14:22 ICT | Task 30 PDPA compliance is now implemented as a backend-first privacy slice plus additive settings UI: added a new `PrivacyModule` with JWT-protected `/users/me`, `/users/me/consent`, `/users/me/privacy-requests`, and `/users/me/data-export*` endpoints; append-only consent history; 30-day SLA-backed rights requests; ZIP-based JSON/CSV portability exports; a global redacting logger for PII-safe logs; a documented 72-hour PDPA breach runbook; and a settings-page privacy card for consent/export actions. Focused privacy/logging unit tests, privacy e2e, `db:generate`, `prisma migrate deploy`, backend lint/typecheck/build, and targeted frontend test/lint/typecheck/build all pass | Natural follow-up is admin-side fulfillment workflow for privacy requests if you want Task 30 extended from request intake into reviewed execution
2026-03-28 21:14 ICT | Closed the remaining Task 30 gaps: privacy requests now have admin-only fulfillment routes that execute correction/export/withdrawal/anonymization actions, PDPA breach incidents are persisted with automatic mandatory SES email delivery to the PDPA Office and affected users, and a real Postgres-backed `PrismaPrivacyStore` spec now proves the new SQL path. Focused privacy/notification unit tests, admin privacy e2e, `db:generate`, `prisma migrate deploy`, DB-backed privacy store tests, backend lint/typecheck/build all pass | Task 30 now has no remaining implementation gap inside the current backend scope
2026-03-28 22:49 ICT | Frontend de-mocking pass is now effectively complete ahead of Task 32: login/session, dashboard, analytics, lanes, lane creation, checkpoint capture, settings/privacy, rules admin, lane detail, and partner submissions are all wired to live backend contracts; lane detail now loads real proof packs and temperature summaries, and the partner screen proxies real API-key validation plus lab/logistics submissions instead of placeholder copy. Focused lane-detail/partner/unit tests, full frontend Jest, frontend lint/typecheck/build, backend lint/typecheck/build, and placeholder/mock import scans all pass | Task 32 can now target real frontend journeys instead of a mixed mock/live surface
2026-03-29 10:03 ICT | Task 32 hardening batch is partially closed: reconciled stale Task Master statuses for merged frontend/privacy work, added a real DB-backed audit-chain integrity e2e with 1000-entry verification + tamper detection, locked down the audit routes behind JWT + lane ownership, and extended `Security Scan` with app-booted OWASP ZAP baseline scans plus a high-risk-only report gate. Focused audit unit/e2e, lint, typecheck, build, YAML parse, and ZAP parser smoke all pass; local Docker-backed ZAP validation also exposed and fixed an incorrect report-path bug in the workflow | Remaining Task 32 scope is `32.1` k6 and `32.3` Playwright critical-flow coverage
2026-03-29 10:33 ICT | Closed the last Task 32 confidence gap by extracting the ZAP path into shared repo scripts (`security:zap`, `security:zap:local`), removing the Linux-only networking assumption from the scan targets, and validating the exact portable local+CI scan flow end-to-end against live backend/frontend surfaces. The final local run completed with zero high-risk findings; only warning-level missing-header findings remain outside the current high-risk merge gate | Task 32 now only has `32.1` k6 and `32.3` Playwright left
2026-03-29 10:46 ICT | PR #38 surfaced one more real CI-only security-scan bug: the ZAP container user could not create its generated `zap.yaml` inside a repo-root bind mount on GitHub runners. The shared `scripts/run-zap-baseline.sh` path now mounts only a dedicated scan output directory, makes it writable for the container user, and keeps report parsing on the host side unchanged; shell syntax and a direct container write smoke both pass locally | Let CI rerun the `Security Scan` job and confirm the shared path is now green on GitHub-hosted Linux
2026-03-29 13:06 ICT | Closed the remaining Task 32 scope with real Playwright critical-flow coverage and dockerized k6 smoke/load scripts wired into CI. Live browser/perf runs surfaced and fixed four production bugs: evidence lineage `TEXT` ids were incorrectly cast to UUIDs, malformed checkpoint-photo EXIF failures were leaking as 500s, EXIF GPS extraction was silently broken by an over-restrictive `pick` option, and concurrent lane creation could race public lane/batch ID allocation. I also made `prisma/seed.ts` idempotent so repeat local smoke runs do not fail on duplicate `LN-2026-001`. Focused backend tests, frontend Playwright, frontend lint/typecheck/build, local k6 smoke, backend lint/typecheck/build, and repeat seed runs all pass | Task 32 is fully done; next safest batch is whichever post-hardening product scope you want to take on after submission/merge
2026-03-29 13:11 ICT | PR #39 Playwright CI exposed one final harness-only auth issue: production-mode Next was setting `Secure` session cookies by default while the helper `APIRequestContext` exercised `/api/zrl/...` over plain `http://127.0.0.1`, so the helper lost its authenticated session in GitHub Actions. Added an explicit `AUTH_COOKIE_SECURE` override in the frontend session utility, covered it with a focused frontend unit test, and set the override only for the CI Playwright frontend start step. Production-mode local smoke now proves `POST /api/session/login` plus cookie-backed `GET /api/zrl/lanes/LN-2026-001/checkpoints` succeed over HTTP, and the full local Playwright suite is green again | Resubmit PR #39 and let the rerun confirm the Playwright job is now green on GitHub-hosted Linux
2026-03-29 14:09 ICT | Reconciled stale Task Master state by closing already-implemented Task 12, then completed Task 15 cold-chain SLA reporting: added `GET /lanes/:id/temperature/sla`, chart-ready report payloads with optimal-band/checkpoint/excursion-zone data, checkpoint marker store reads from `checkpoints`, and mocked plus live Postgres-backed e2e coverage. Focused cold-chain unit tests, mocked cold-chain e2e, live DB cold-chain e2e, lint, typecheck, and build all pass | Next safest product batch is Task 16 on top of the now-complete proof-pack + cold-chain reporting foundation
2026-04-02 17:30 ICT | Reconciled the human progress log against Task Master: the board is now at 32/33 top-level tasks done (96.97%), 0 in progress, with Task 20 `Partner API Integrations (Lab & Logistics Adapters)` as the only remaining pending task; its remaining subtasks are the Central Lab Thai adapter, SGS adapter, Thai Airways/Kerry logistics adapters, and ACFS GAP lookup | Start Task 20 when ready; Task Master and `docs/PROGRESS.md` are now back in sync on current overall project status
2026-04-02 17:43 ICT | Confirmed the progress state against the codebase itself: `src/app.module.ts` wires the completed runtime modules (lane, rules-engine, evidence, cold-chain, dispute, mrv-lite, notifications, privacy, analytics), partner ingestion is live via `src/modules/evidence/evidence.controller.ts` and the frontend partner proxy/UI, and CI/perf/security assets are on disk under `.github/workflows/ci.yml`, `performance/k6/`, `frontend/e2e/`, and `scripts/run-zap-baseline.sh`. Task 20 remains correctly pending because `src/integrations/lab-api`, `src/integrations/logistics`, and `src/integrations/telemetry` are present but still contain no implementation files and no integration module is wired into `AppModule` | The next real product task remains Task 20 unless you want to split that adapter work into smaller execution batches first
2026-04-02 17:57 ICT | Task 20 done: added a real `PartnerIntegrationsModule` with provider-specific import/lookup routes for Central Lab Thai, SGS Thailand, Thai Airways, Kerry, and ACFS; implemented retry/backoff, rate limiting, and ACFS lookup caching; extended evidence imports so partner telemetry also feeds the cold-chain ingestion pipeline and ACFS imports create `GAP_CERT` artifacts; and wired the existing frontend lane-creation GAP field into pre-create ACFS validation plus post-create GAP import. Focused backend unit/e2e, frontend unit, backend/frontend lint, backend/frontend typecheck, and backend/frontend build all pass. Task Master now shows 33/33 top-level tasks complete. | Remaining housekeeping is optional only: reconcile older stale subtask records outside Task 20 if you want the Task Master subtask percentage to also read 100%
2026-04-02 22:00 ICT | Closed the remaining stale/partial subtask gaps against the codebase: MRV-Lite now persists `EmissionFactor` seed data and drives ESG waste metrics from lane/dispute state, proof-pack completion now auto-reconciles validated lanes into `PACKED`, and Dispute Shield now has a dedicated `DisputeTimelineService`, richer defense-pack payloads, and a six-section `templates/defense.hbs` that renders reconstructed timeline, temperature forensics, photo EXIF evidence, and audit trail. Focused unit suites, focused e2e (`mrv-lite`, `dispute`), `db:generate`, lint, typecheck, and build all pass; Task Master is now fully reconciled at 33/33 top-level tasks and 120/120 subtasks complete. | Product board and codebase are now back in full sync unless you want a final PR/merge pass for this reconciliation batch
2026-04-04 13:32 ICT | Restored the lane wizard’s explicit cold-chain config path so `Logger` and `Telemetry` now collect `Device ID` plus `Data Frequency (seconds)`, client-side validation mirrors backend rules, review-step rows expose stable test ids, and lane-create payloads send the richer `coldChainConfig` fields expected by the domain model. Focused frontend unit tests (`21/21`), frontend lint, and frontend typecheck all pass. A targeted Playwright repro confirms the old route-step timeout is fixed, but the live create-lane submit still ends on a backend `500 Internal server error` that needs separate server-side debugging. | Debug the backend lane-create `500` on the live mango/japan/logger flow, then rerun the targeted Playwright scenario and expand back out to the lane-creation matrix
2026-04-04 14:46 ICT | Closed the backend follow-up from the lane wizard work: the live `POST /api/zrl/lanes` failure was caused by a stale repository ruleset at `rules/eu/mango.yaml` that lacked the now-required shared `metadata` schema, so rule-loader cache refresh threw `Error: Invalid metadata.` during lane creation. Updated the EU mango YAML to the shared rule-pack format, set its lab policy to truthful `DOCUMENT_ONLY`, added a repository regression test for that file, and verified the fix with focused rules-loader tests, backend typecheck/build, direct authenticated `curl`, and a green targeted Playwright `mango-japan-air` flow. | Expand from the single supported Playwright repro back out to the broader lane-creation matrix or batch the frontend/backend fixes into a PR
2026-04-04 14:51 ICT | Finished the first exhaustive pack in the rule-authoring sequence: `rules/korea/mango.yaml` and `rules/korea/mango-substances.csv` were re-verified against current official QIA and MFDS source pages, stale “deferred CSV” language was removed, the pack was promoted from `PRIMARY_PARTIAL` to `FULL_EXHAUSTIVE`, retrieval metadata was refreshed to `2026-04-04`, and the structured VHT check now captures overseas inspection / registration requirements in addition to `47℃` for `20` minutes. Focused rules-loader tests, rules-engine e2e, admin rules-page tests, and backend/frontend typechecks all pass. | Move to `JAPAN/MANGO` next and replace its curated high-risk subset with a full commodity-specific official pack
2026-04-04 16:27 ICT | Hardened `JAPAN/MANGO` with exact MAFF Thailand mango phytosanitary/VHT metadata, truthful `PRIMARY_PLUS_SECONDARY` pesticide-source labeling, refreshed source headers, and focused rules-loader/rules-engine contract coverage. The pack now clearly states that its 12 pesticide rows are still a curated high-risk subset rather than a full commodity table. | Continue toward exhaustive `JAPAN/MANGO` only after capturing a defensible commodity-complete Japan mango MRL row set
2026-04-04 16:49 ICT | Finished `JAPAN/MANGO` as the second exhaustive pack in the current rule-authoring sequence: replaced the 12-row curated CSV with the full 191-row JFCRF mango commodity snapshot, promoted the pack to `FULL_EXHAUSTIVE`, and added explicit qualifier handling for the two non-numeric source rows (`N.D.` and physiological-level `※`) so runtime validation stays truthful and fail-closed. Focused rule-loader tests, rules-engine unit/e2e, backend typecheck/lint, and frontend typecheck/lint all pass. | Move to `JAPAN/DURIAN` next
2026-04-04 19:14 ICT | Finished `JAPAN/DURIAN` as the third exhaustive pack in the current rule-authoring sequence: replaced the old 10-row proxy/monitoring CSV with the actual durian-applicable JFCRF commodity snapshot (`その他の果実(アセロラを除く。)` / code `25839`), promoted the pack to `FULL_EXHAUSTIVE`, and grounded the non-pesticide side in MAFF’s import-condition database result for `ドリアン (Durio zibethinus)` fresh fruit, which shows normal inspection with no special Annex quarantine treatment. The explicit row set is intentionally only one physiological-level `GIBBERELLIN` row, with all other pesticides still governed by Japan’s `0.01 mg/kg` positive-list fallback. Focused rules-loader tests, backend typecheck, and focused lint all pass. | Move to `JAPAN/MANGOSTEEN` next
2026-04-04 19:34 ICT | Reconnected the previously local-only Playwright lane-creation harness to current `main`: added local-only `webServer` startup in `frontend/playwright.config.ts`, promoted `3400/3401` as the default local browser/backend ports for that harness, added explicit root/browser Playwright scripts, and validated the restored lane-matrix files plus `scripts/run-local-playwright.sh`. `bash -n scripts/run-local-playwright.sh`, `npm run test:e2e:playwright:list`, frontend lint, frontend typecheck, and a green targeted `mango-japan-air` Playwright run all passed. | Isolate the harness slice from the dirty tree, run formal review, and submit it as its own PR before resuming `JAPAN/MANGOSTEEN`
2026-04-04 22:39 ICT | Finished `JAPAN/MANGOSTEEN` as the fourth exhaustive pack in the current rule-authoring sequence: replaced the old 11-row proxy/study CSV with the full mangosteen-applicable JFCRF `11900` export snapshot (`248` official rows minus the single acerola-only `GIBBERELLIN` row = `247` explicit rows), promoted the pack to `FULL_EXHAUSTIVE`, and corrected the non-pesticide side from a false VHT-exemption claim to MAFF’s actual Thai mangosteen steam-heat / phytosanitary procedure. Focused rules-loader tests, rules-engine e2e, backend typecheck, and focused lint all pass. | Move to `JAPAN/LONGAN` source check next
2026-04-04 22:55 ICT | Corrected `JAPAN/LONGAN` to fail closed instead of fabricating an exhaustive Thailand-origin pack: removed the proxy Japan longan YAML/CSV after confirming MAFF’s current conditional-release list supports `Thailand mango`, `Thailand mangosteen`, and `Vietnam longan`, but not `Thailand longan`, and that MAFF’s Thailand import/traveler guidance still lists `リュウガン` as prohibited from Thailand. Added focused backend/frontend coverage so the loader now returns no Japan longan pack and the lane-creation harness classifies `longan-japan-air` as unsupported from rule-file absence. | Next substantive pack work depends on new primary-source evidence
2026-04-04 23:31 ICT | Finished `KOREA/DURIAN` as the second Korea pack and the next supported post-mango Korea slice: added a direct QIA/MFDS durian rule pack using QIA’s plain Thai durian phytosanitary path plus MFDS commodity code `ap105051059`, with the full current explicit Korea durian row set (`3` rows) and the official `0.01 mg/kg` fallback for all unlisted pesticides. Focused rules-loader tests, frontend harness-support tests, backend typecheck, frontend typecheck, and frontend lint all pass. | Move to `KOREA/MANGOSTEEN` next
2026-04-05 02:01 ICT | Finished `KOREA/MANGOSTEEN` as the next Korea pack: added a direct QIA/MFDS mangosteen rule pack using QIA’s Thai mangosteen conditional-import path plus MFDS commodity code `ap105051360`, with the full current explicit Korea mangosteen row set (`3` rows) and the official `0.01 mg/kg` fallback for all unlisted pesticides. Modeled mandatory MB fumigation truthfully on the phytosanitary-certificate path because the current QIA page requires the certificate to carry fumigation details rather than a separate VHT-style document. Focused rules-loader tests passed on RED/GREEN plus 3 repeated GREEN runs, and backend typecheck/lint both pass. | Move to the next supported Korea pack or widen evidence modeling only if a future pack needs a first-class fumigation artifact
2026-04-05 06:58 ICT | Confirmed `KOREA/LONGAN` must remain unsupported for Thailand-origin fresh fruit on current official Korea sources: QIA’s fresh-fruit import-conditions page still omits Thai longan from both the general importable list and the conditional-import list, while MFDS still exposes longan residue rows under commodity code `ap105051020`. Added explicit repository and lane-harness assertions so the repo fails closed on Korea longan instead of drifting into a speculative rule pack. | Continue rule-authoring only for market/product pairs with direct primary-source import support
2026-04-05 11:03 ICT | Finished `EU/MANGO` as a truthful exhaustive pack from current official Commission sources: upgraded the old partial placeholder to product code `0163030` with a full Commission API snapshot (`520` raw mango rows merged into `516` unique operational rows), promoted lab enforcement to `FULL_PESTICIDE` with the EU `0.01 mg/kg` fallback, and added explicit `NO_NUMERIC_LIMIT` handling for the `13` current no-numeric mango rows so validation stays truthful and fail-closed. Focused rules-loader/runtime tests, frontend harness unit coverage, backend/frontend lint + typecheck, and a green targeted Playwright `mango-eu-air` run all passed. | Continue EU source-truth authoring for the next supported fruit pack
2026-04-05 12:45 ICT | Reduced PR-to-merge friction without weakening gate coverage: CI now cancels superseded PR runs, no longer serializes independent `Build`/integration/playwright/perf jobs behind fake `needs`, and uses explicit Node cache paths for both root and frontend lockfiles; advisory Claude/auto-approve workflows now also cancel stale PR-head runs; repo-local fast staged-file pre-commit hooks plus an explicit full-validation script were added; repository auto-merge is enabled and verified via GitHub API. | Fresh clones should run `bash scripts/install-git-hooks.sh`, and PRs can now be set to auto-merge once required checks pass
2026-04-06 20:35 ICT | Tracked the repo-local hook/validation scripts in Git, tightened the rules engine so measured `PHYSIOLOGICAL_LEVEL` / `NO_NUMERIC_LIMIT` rows block aggregate lab validation instead of returning a misleading overall `PASS`, and added `EU/DURIAN` as the next exhaustive Commission-backed pack (`product 83` / `0163100`, `519` raw rows merged into `516` unique operational rows). Focused rules-loader/runtime tests, frontend lane-support coverage, and backend typecheck all pass. | Decide whether the Claude review workflow should keep requiring admin merge when it edits itself, or be reworked to avoid self-validation deadlocks
2026-04-06 21:40 ICT | Finished `EU/MANGOSTEEN` as the next truthful exhaustive EU pack while staying explicit about Commission taxonomy limits: added a new `rules/eu/mangosteen.yaml` plus the full current Commission `product 77` snapshot (`Papayas / Carica papaya`, code `0163040`) because the official EU pesticides database currently covers mangosteen through that synonym/scientific-name leaf rather than a standalone mangosteen node. The checked-in CSV captures `522` raw rows merged into `516` operational rows with `13` explicit `NO_NUMERIC_LIMIT` cases, and the frontend lane-creation harness now classifies `mangosteen-eu-truck` as supported from the real rule-file presence. | Continue EU source-truth authoring only where the official Commission taxonomy provides a defensible market/product mapping
2026-04-07 13:50 ICT | Completed Task Master `34.1` for the open-source OCR/form-completeness track: added a new machine-readable `rules/document-matrix.yaml` covering the current first-pass official/formal artifact subset (`PHYTO_CERT`, `VHT_CERT`, `MRL_TEST`, `GAP_CERT`, `INVOICE`) across all `9` supported live market/product rule packs, including canonical required field keys, authority source URLs, combo-specific treatment-field overrides, and explicit confidence notes for ambiguous labels like `VHT Certificate` and `Export License`. Added a typed backend loader plus unit tests that cross-check the matrix against the live `rules/*.yaml` tree so the matrix cannot silently drift from supported combos. | Move to Task Master `34.2` to add derived analysis storage/types before wiring OCR providers
2026-04-07 14:00 ICT | Completed Task Master `34.2` for the OCR/form-completeness track: added additive persistence and typed models for derived artifact analysis without mutating the source evidence/hash model. A new `evidence_artifact_analyses` table plus Prisma model/migration now provides lane-linked storage for analyzer version, status, document classification, extracted fields, missing/low-confidence field keys, and timestamps; evidence artifact store/service contracts now expose an optional latest analysis alongside existing artifact metadata. Focused evidence/matrix tests and backend typecheck pass. | Move to Task Master `34.3` to add the open-source OCR provider abstraction and local extraction flow
2026-04-07 14:12 ICT | Completed Task Master `34.3` for the OCR/form-completeness track: added a new `EvidenceDocumentAnalysisProvider` abstraction plus a local `TesseractEvidenceDocumentAnalysisProvider` implementation, wired into `EvidenceModule` behind a new DI token. The provider reports binary availability, detects optional OCRmyPDF preprocessing support, and extracts text through local `tesseract` with explicit language selection while failing cleanly when OCR is unavailable. Focused OCR provider tests and backend typecheck pass; upload/reanalysis orchestration is intentionally deferred to the next subtask. | Move to Task Master `34.4` / `34.6` depending on whether you want classifier/extractor logic or upload-time analysis wiring first
2026-04-07 14:24 ICT | Completed Task Master `34.4` for the OCR/form-completeness track: added a matrix-driven evidence document classifier/extractor service that consumes `rules/document-matrix.yaml` and deterministically classifies first-pass supported artifacts by combo + artifact type + OCR text, then extracts normalized fields using conservative regexes and existing metadata names (`certificateNumber`, `issuer`, `expiryDate`, `results`, etc.). The classifier returns document role, confidence, extracted fields, and explicit missing/low-confidence field keys, and is wired into `EvidenceModule` behind a dedicated DI token for later upload orchestration. Focused classifier/OCR tests and backend typecheck pass. | Move to Task Master `34.6` to trigger OCR + classification during upload/reanalysis and persist analysis rows
2026-04-07 14:40 ICT | Completed Task Master `34.6` for the OCR/form-completeness track: evidence uploads now attempt derived OCR/classification after persistence and integrity verification, a new `POST /evidence/:id/reanalyze` path reruns the same analysis flow on demand, and derived analysis rows are persisted through the evidence store into `evidence_artifact_analyses` without mutating source evidence records. Focused evidence OCR/classifier/service tests and backend typecheck pass; analysis remains fail-open when OCR is unavailable or lane rule context is missing. | Next natural step is to surface persisted analysis/completeness results in the UI or formalize the remaining `34.5` semantics if stricter matrix-level completeness scoring is still needed
2026-04-07 14:50 ICT | Completed Task Master `34.5` for the OCR/form-completeness track: each derived artifact analysis now carries a formal per-document field-completeness result driven by `rules/document-matrix.yaml`, including expected, present, missing, low-confidence, and unsupported field keys plus matrix version and support status. This remains additive and separate from existing lane completeness scoring; new completeness payloads are persisted in `evidence_artifact_analyses.field_completeness`, mapped through the evidence store/service responses, and validated with focused classifier/service/store tests, Prisma generate, and backend typecheck. | Next natural step is frontend surfacing of `latestAnalysis.fieldCompleteness` for analyst review, since the backend semantics are now explicit and versioned
2026-04-07 15:00 ICT | Completed the current frontend/operator slice of Task Master `34.8`: the lane detail Evidence tab now surfaces `latestAnalysis` document detection, confidence, summary text, and field completeness (`present`, `missing`, `low confidence`, `unsupported extract`) without replacing existing integrity-status UX. The same tab now also supports single-artifact `Reanalyze` and bulk `Backfill Missing Analysis` actions that call the existing `POST /evidence/:id/reanalyze` path for artifacts missing analysis or missing field-completeness payloads. Focused frontend evidence/lane-detail tests, frontend lint, and frontend typecheck all pass. | Remaining OCR track work is `34.7` rules/certification integration and `34.9` fixtures/operator documentation; UI can be iterated later if lane compliance needs a dedicated aggregate analysis panel
2026-04-08 16:52 ICT | Implemented OCR readiness slice `34.9.4`: added a typed loader for the committed OCR fixture manifest plus SVG-to-text helpers, switched the document-matrix guards to use that shared loader, and converted the classifier suite to consume committed fixture data for the base phyto and Japan-mango phyto override paths. Tightened the phyto extractor just enough to read package/commodity/officer fields and explicit treatment-reference lines from the committed fixtures. Focused OCR manifest/matrix/classifier tests and backend typecheck all pass. | Continue with the next OCR readiness slice by expanding manifest-backed classifier coverage across the remaining labels and override variants
2026-04-08 17:32 ICT | Implemented backend OCR integration slice `34.7.1`: certification consumers now fall back to persisted OCR-extracted expiry fields when source artifact metadata lacks expiry information. The change threads latest analysis extracted fields into lane evaluation, upload-time certification alerts, and scheduled certification-expiry scans while preserving metadata-first behavior. Focused rules-engine/evidence/lane unit tests and backend typecheck all pass. | Continue with `34.7.2` to apply the same extracted-field integration pattern to the next smallest backend compliance consumer
2026-04-08 18:17 ICT | Implemented backend OCR integration slice `34.7.2`: MRL/lab validation now distinguishes between an unproven `MRL_TEST` artifact and an OCR-proven `MRL Test Results` document when structured numeric metadata is missing. The rules engine remains fail-closed: OCR can prove document presence and move the outcome from `MRL_TEST_REQUIRED` to `MRL_RESULTS_REQUIRED`, but it still does not infer numeric pesticide PASS/FAIL values from OCR text alone. Focused rules-engine/evidence/store tests and backend typecheck pass. | Continue with `34.7.3` to let OCR document labels satisfy invoice-family trade-document checklist items when metadata labeling is absent
2026-04-08 18:32 ICT | Implemented backend OCR integration slice `34.7.3`: invoice-family checklist satisfaction now falls back to persisted OCR `documentLabel` when metadata labeling is absent, and the previous broad behavior where any `INVOICE` artifact satisfied every invoice-family requirement was removed. Upload-time and lane-read completeness paths now both receive OCR trade-document labels, and focused rules/evidence/store/lane tests plus backend typecheck pass. | Continue with `34.7.4` to make manual reanalysis refresh lane completeness and OCR-backed compliance consequences immediately
2026-04-08 18:39 ICT | Implemented OCR readiness slice `34.9.5`: expanded manifest-backed classifier coverage beyond the initial phyto slice to the remaining committed base non-override labels (`MRL Test Results`, `GAP Certificate`, and the invoice-family trade docs) plus one representative VHT override path (`KOREA/MANGO`). The classifier now extracts the structured fields present in those committed fixtures with tighter regexes that avoid cross-document leakage. Focused OCR manifest/matrix/classifier tests and backend typecheck pass. | Continue with `34.7.4` per the execution order, then return later to the remaining OCR override coverage and browser readiness slices
2026-04-08 18:46 ICT | Implemented backend OCR integration slice `34.7.4`: `POST /evidence/:id/reanalyze` now refreshes lane completeness, republishes the lane completeness event, reruns lane transition reconciliation, and rechecks certification alerts using the fresh OCR analysis fields instead of only writing a new analysis row. Focused evidence/rules/lane unit tests and backend typecheck pass. | Continue with `34.7.5` to surface OCR-influenced compliance provenance in backend contracts
2026-04-08 18:54 ICT | Implemented backend OCR integration slice `34.7.5`: lane completeness responses now expose additive provenance for checklist satisfaction, lab-validation evidence source, and certification expiry source, making OCR-influenced backend decisions explicit without changing persistence or route wiring. Focused rules/lane/evidence unit tests and backend typecheck pass. | Continue with the remaining `34.9` readiness slices, which can now rely on backend provenance instead of inferring OCR influence indirectly
2026-04-08 19:27 ICT | Implemented OCR readiness slice `34.9.6`: added manifest-backed classifier coverage for the remaining committed override fixtures (`JAPAN/MANGOSTEEN` and `KOREA/MANGOSTEEN` phyto, plus `JAPAN/MANGO` and `JAPAN/MANGOSTEEN` VHT) and extended the classifier to extract their required combo-specific override fields. Focused OCR manifest/matrix/classifier tests and backend typecheck pass. | Continue with the remaining `34.9` browser-readiness and readiness-accounting slices
2026-04-08 19:32 ICT | Implemented OCR readiness slice `34.9.7`: the existing Playwright OCR smoke test now uploads a real committed OCR fixture via a shared frontend helper instead of generating a one-off image inline, and the new helper is covered by a focused frontend unit test. Frontend unit test, Playwright test listing, lint, and frontend typecheck all pass. | Continue with `34.9.8` to add broader browser OCR matrix coverage using the committed fixture corpus
2026-04-08 20:03 ICT | Implemented OCR readiness slice `34.9.8`: added `frontend/e2e/evidence-ocr-matrix.spec.ts` with one committed-fixture browser OCR path mapped to each supported combo, extended the frontend OCR asset helper to render committed SVG fixtures into PNG uploads for the live browser OCR path, and widened the backend helper contract so browser tests can assert detected document labels as well as field completeness. Frontend unit/lint/typecheck and focused live Playwright matrix runs for representative phyto and VHT cases pass. | Continue with `34.9.9` to add readiness accounting/reporting so READY=yes becomes machine-checkable
2026-04-09 10:38 ICT | Added `docs/OCR-RUNTIME-SCOPE-REFERENCE.md` to clarify the current OCR/form-completeness contract: glossary of certificates/organizations, what `rules/document-matrix.yaml` defines, what the current runtime checks vs does not check yet, how the exhaustive rule-data harness differs from document-derived runtime validation, supported upload file types, and where users currently upload evidence. | Use this reference when scoping the stricter readiness/reporting work and any future expansion toward full document-derived PASS/FAIL
2026-04-09 10:47 ICT | Expanded `docs/OCR-RUNTIME-SCOPE-REFERENCE.md` with a compact `Current Scope vs Future Scope` table so the boundary between current first-pass OCR readiness and future full document-derived compliance work is immediately scannable. | Reuse this table when defining `34.9.9+` strict readiness criteria and any future matrix expansion
2026-04-09 10:55 ICT | Expanded `docs/OCR-RUNTIME-SCOPE-REFERENCE.md` again with a document-by-document runtime reference table covering each current form/certificate/artifact family, its artifact type, practical upload formats, and whether current runtime behavior depends on OCR, metadata, or structured ingestion. | Use this table as the quick operational reference when deciding which evidence families need richer OCR, metadata, or browser-proof expansion next
2026-04-09 11:01 ICT | Implemented OCR readiness slice `34.9.9`: added a strict machine-checkable readiness ledger derived from the live document matrix and committed fixture manifest, with explicit classifier/backend/browser proof status for every current required `combo x document` slot. The ledger currently counts `75` required slots and truthfully reports `0` fully-ready slots under the stricter standard because browser proof is still representative rather than exhaustive. Focused evidence readiness tests and backend typecheck pass. | Continue with `34.9.10` to expand browser proof from representative combo coverage to every required document slot
2026-04-09 11:21 ICT | Implemented OCR readiness slice `34.9.10`: expanded the browser OCR matrix from representative combo coverage to all `75` current required `combo x document` slots using a canonical frontend slot generator backed by the committed fixture manifest and live lane-creation scenarios. Frontend unit/lint/typecheck and focused live browser slot runs for invoice-family and VHT cases pass. | Decide whether extra support letters/certificates should become canonical matrix scope; if so, model them explicitly before claiming broader readiness
2026-04-09 11:26 ICT | Fully executed the exhaustive `34.9.10` browser OCR matrix: `npm run test:e2e -- --project=chromium e2e/evidence-ocr-matrix.spec.ts` passed with all `76` tests green (`75` required browser OCR slots plus the slot-count assertion). This closes the current first-pass browser-proof breadth gap under the stricter readiness standard. | Remaining OCR scope is only whether extra supporting forms/certificates beyond the current matrix should be modeled and proven
2026-04-09 11:30 ICT | Implemented OCR readiness slice `34.9.11` as a scope-expansion audit: the live rule packs and current first-pass matrix were compared directly, and no additional standalone OCR form families were justified beyond the existing first-pass matrix labels. Added backend audit/tests plus docs (`OCR-SCOPE-EXPANSION-AUDIT.md` and OCR scope reference updates) to make that boundary explicit and machine-checked. | Current first-pass OCR readiness scope is now fully defined; any future extra support-letter/certificate work must begin with explicit matrix modeling
2026-04-09 16:45 ICT | Realigned OCR expansion planning with actual Task Master structure: added Task Master subtask `34.10` for combo-by-combo external research and explicit modeling of any newly justified supporting forms/certificates, instead of continuing the earlier pseudo-nested `34.9.11` label. Updated OCR scope docs so the existing audit remains the truthful current-state baseline while future expansion is tracked as a separate research-backed follow-on. | Use `34.10` for any internet-backed document-scope expansion; treat the earlier scope audit as baseline evidence, not as the final word on real-world extra controls
# 2026-04-13

- Fixed the OCR review follow-up gaps without changing matrix product scope: restored override parity against shared+override matrix fields, added end-to-end phyto extraction/support for `declaredPointOfEntry` and `officialSealOrSignature`, stabilized the Evidence smoke test with artifact-scoped selectors, and aligned override fixtures/manifests/browser/backend tests around explicit full completeness.
